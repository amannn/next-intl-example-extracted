# next-intl-example-extracted

An example that showcases an app with message extraction set up.

```bash
# Install dependencies
npm install

# Run the dev server (extracts messages on the fly)
npm run dev

# Build the app (extracts messages upfront)
npm run build

# Run the tests (consumes useExtracted without transformation)
npm run test
```
